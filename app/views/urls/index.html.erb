<%= link_to "Shorten A Url", new_url_path %>
<ul>
<% @urls.each do |url| %>
   <li id="<%= url.id %>" class="short_url"><%= link_to "http://localhost:3000/#{url.short_url}", root_path(url.short_url) %></li>
<% end %>
</ul>
